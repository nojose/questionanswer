<%= form_for(@qstatus) do |f| %>
  <% if @qstatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qstatus.errors.count, "error") %> prohibited this qstatus from being saved:</h2>

      <ul>
      <% @qstatus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :qtype %><br />
    <%= f.text_field :qtype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
